kind = "web"
previewPath = "/"
title = "THREADCAST — World of Aethros"
version = "1.0.0"
id = "artifacts/threadcast"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20390

[services.development]
run = "pnpm --filter @workspace/threadcast run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/threadcast", "run", "build" ]
publicDir = "artifacts/threadcast/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20390"
BASE_PATH = "/"
